data = list(input().split())
con = data[0]
STR = data[1:]
result = str()
for i in range(len(STR)):
    result+=STR[i]
    result+= " "
    if i != len(STR)- 1:
        result+=con
        result+= " "
print(result)
